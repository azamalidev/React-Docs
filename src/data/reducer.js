import useReducer from '../assets/image/reducer.png';
const Reducer = {
  image: useReducer,
  title: ['useReducer'],
  concept: [
    'Reducer Function: A function that determines how the state should change in response to an action. It takes the current state and an action as arguments and returns the new state.',
    'Initial State: The starting value of the state.',
    'Dispatch: A function provided by useReducer to send actions to the reducer function. When an action is dispatched, the reducer processes it and returns the updated state.',
  ],
  paragraph: [
    `useReducer is a React hook that provides a way to manage complex state logic in function components. It's an alternative to useState and is especially useful when the state logic involves multiple sub-values or when the next state depends on the previous state.`,
  ],
  example: [
    'Let’s break down the example 👨‍💻',
  ],
  code: [
    "import React, { useReducer } from 'react';",
    'const UseReducerPage = () => {',
    '  const initialState = { count: 0 };',
    '  const countReducer = (state, action) => {',
    '    switch (action.type) {',
    "      case 'increment':",
    '        return { count: state.count + 1 };',
    "      case 'decrement':",
    '        return { count: state.count - 1 };',
    '      default:',
    '        throw new Error();',
    '    }',
    '  };',
    '  const [state, dispatch] = useReducer(countReducer, initialState);',
    '',
    '  const increment = () => {',
    "    dispatch({ type: 'increment' });",
    '  };',
    '  const decrement = () => {',
    "    dispatch({ type: 'decrement' });",
    '  };',
    '  return (',
    '    <div>',
    "      <h5 className='reducerCounter_title'> let's Play with Counter App 😎</h5>;",
    "      <div className='reducerCounter'>",
    '        <span onClick={decrement}>',
    "          <Button content={'-'} />",
    '        </span>',
    '        <span>{state.count}</span>',
    '        <span onClick={increment}>',
    "          <Button content={'+'} />",
    '        </span>',
    '      </div>',
    '    </div>',
    '  );',
    '};',
  ],
};
export default Reducer;
